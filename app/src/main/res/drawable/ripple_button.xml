<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/light_blue_ivy"> <!-- Replace with your desired ripple color -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="10dp" /> <!-- Adjust the radius value to match the corner radius of your button -->
            <solid android:color="@color/blue_ivy" /> <!-- Set the background color for the button -->
        </shape>
    </item>
</ripple>